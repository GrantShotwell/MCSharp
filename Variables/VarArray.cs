using MCSharp.Compilation;
using System;
using System.Collections.Generic;
using System.Text;

namespace MCSharp.Variables {
    //public class VarArray<TVariable> : Variable, IList<TVariable> where TVariable : Variable {

    //    public override string TypeName => $"ARRAY";
    //    public override ICollection<Access> AllowedAccessModifiers => new Access[] { Access.Public, Access.Private };
    //    public override ICollection<Usage> AllowedUsageModifiers => new Usage[] { Usage.Default, Usage.Static };

    //    private TVariable[] Array { get; set; }

    //    public VarArray() : base() {}
    //    public VarArray(Access access, Usage usage, string name, Compiler.Scope scope) : base(access, usage, name, scope) { }

    //    public override Variable Initialize(Access access, Usage usage, string name, Compiler.Scope scope, ScriptTrace trace) => new VarArray<Variable>(access, usage, name, scope);
        
    //    public override Variable Construct(ArgumentInfo passed) {
            


    //    }

    //}

}
